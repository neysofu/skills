interface:
  display_name: "PDF to Markdown"
  short_description: "Convert native-text PDFs to Markdown"
  default_prompt: "Use $pdf-to-markdown to convert this local PDF into validated Markdown with Firecrawl pdf-inspector."
policy:
  allow_implicit_invocation: false
